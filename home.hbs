<!DOCTYPE html>
<html lang="{{@site.lang}}">
{{> "meta"}}
<body class="{{body_class}}">

   <div class="intro-wrapper">
       <div class="intro-container">
           <span class="site-title mobile-block">
               {{@site.title}}
           </span>
           <span class="site-description">
               {{@site.description}}
           </span>
       </div>
   </div>

   <div class="nav-container">
       {{navigation}}
   </div>

   <div class="form-container" id="join">
     {{> "mc-form" page="home"}}
   </div>

</body>
</html>
